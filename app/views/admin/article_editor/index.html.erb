<div id="article" data-id="<%= @article.id %>" />

<% @template.components.each_with_index do |component, index| %>
    <div class="<%= component.slug %>" data-position="<%= index %>">
        <%= render :partial => component.slug, :locals => {:position => index} %>
    </div>
<% end %>

</div>