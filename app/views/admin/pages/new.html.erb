<div id="stage">
    
    <%= render "left_bar" %>
    
    <%= form_for [:admin, @page], :html => {:id => 'page-form'} do |f| %>
    
        <%= render :partial => 'content', :locals => {:f => f, :action => 'create'}%>
                
        <%= render :partial => 'right_bar', :locals => {:f => f, :action => 'create'} %>
        
    <% end %>
    
</div>
