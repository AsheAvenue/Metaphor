<div id="left-bar">
    <div class="header">
        Categories
    </div>

    <div class="new">
        <%= link_to "New Category »", new_admin_category_path %>
    </div>
    
    <% @categories.each do |category| %>
        <div class="item"><%= link_to category.name, edit_admin_category_path(category) %></div>
    <% end %>
</div>