<div id="left-bar">
    <div class="header">
      <%= Settings.users.plural %>
    </div>

    <div class="new">
        <%= link_to "New #{Settings.users.singular} »", new_admin_user_path %>
    </div>
    
    <% @users.each do |user| %>
        <div class="item"><%= link_to (user.display_name || user.email), edit_admin_user_path(user) %></div>
    <% end %>
</div>