<div id="left-bar">
    <div class="header">
        <%= Settings.collections.plural %>
    </div>

    <div class="new">
        <%= link_to "New #{Settings.collections.singular} »", new_admin_collection_path %>
    </div>
    
    <% @collections.each do |collection| %>
        <div class="item"><%= link_to collection.name, edit_admin_collection_path(collection) %></div>
    <% end %>
</div>