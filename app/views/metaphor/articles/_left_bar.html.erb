<div id="left-bar">
    <div class="header">
        <%= Settings.articles.name.pluralize %>
    </div>

    <div class="new">
        <%= link_to "New #{Settings.articles.name} »", new_article_path %>
    </div>
    
    <% @articles.each do |article| %>
        <div class="item"><%= link_to article.title, edit_article_path(article) %></div>
    <% end %>
</div>